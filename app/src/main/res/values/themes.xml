<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RuskoRadio" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Primary dark color. -->
        <item name="colorPrimaryDark">@color/background_color</item>
        <item name="statusBarBackground">@color/background_color</item>
        <item name="android:windowLightStatusBar">@color/background_color</item>
    </style>

    <style name="Theme.RuskoRadio" parent="Base.Theme.RuskoRadio" />
</resources>